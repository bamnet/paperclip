Usage:

  script/generate DBPaperclip Class attachment1 (attachment2 ...)
  
This will create a migration that will add the proper columns (including one attachment1_file binary column) to your class's table.
